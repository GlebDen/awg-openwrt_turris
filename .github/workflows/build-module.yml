name: Create Release on Tag
# on: [workflow_dispatch]
on:
  push:
    tags:
      - "v*.*.*"

jobs:
  build:
    name: "v${{ matrix.build_env.tag }} - ${{ matrix.build_env.pkgarch}} :: ${{ matrix.build_env.target}}/${{ matrix.build_env.subtarget}} build"
    runs-on: ubuntu-latest
    strategy:
      matrix:
        build_env:
          - tag: "24.10"
            pkgarch: arm_cortex-a9_vfpv3-d16
            target: mvebu
            subtarget: cortexa9
            vermagic: "27e1322291d198b059e40717a0aca6fb"
            # Используем именно тот тег, который вы нашли
            turris_ref: "v9.0.5" 

    steps:
      - uses: actions/checkout@v4
        with:
          # Исправленное имя организации: turris-cz
          repository: turris-cz/openwrt 
          ref: ${{ matrix.build_env.turris_branch }}
          fetch-depth: 0

      - name: Cache Tools and Kernel
        id: cache-tools-kernel
        uses: actions/cache@v4
        env:
          cache-name: "cache-tools-kernel-${{ matrix.build_env.tag }}-${{ matrix.build_env.pkgarch}}-${{ matrix.build_env.target}}-${{ matrix.build_env.subtarget}}"
        with:
          path: "**"
          key: ${{ runner.os }}-build-vm4-${{ env.cache-name }}
          restore-keys: |
            ${{ runner.os }}-build-vm4-cache-tools-kernel-${{ matrix.build_env.tag }}-${{ matrix.build_env.pkgarch}}-

      - name: Building kernel and tools
        run: |
          echo "pkgarch: ${{ matrix.build_env.pkgarch}}, target:${{ matrix.build_env.target}}, subtarget: ${{ matrix.build_env.subtarget}}"

          # 1. Исправленная работа с фидами
          # Если feeds.conf.default нет, создаем feeds.conf с нуля или используем существующий
          if [ -f feeds.conf.default ]; then
            cp feeds.conf.default feeds.conf
          elif [ ! -f feeds.conf ]; then
            touch feeds.conf
          fi
          
          echo "src-git awgopenwrt https://github.com/yury-sannikov/awg-openwrt.git" >> ./feeds.conf
          ./scripts/feeds update && ./scripts/feeds install -a

          # 2. Настройка конфигурации для Turris Omnia
          # Вместо make defconfig используем специфический конфиг для mvebu, если он есть
          if [ -f target/linux/${{ matrix.build_env.target }}/config-6.6 ]; then
            cp target/linux/${{ matrix.build_env.target }}/config-6.6 .config
          fi
          
          # Добавляем параметры AmneziaWG
          echo "CONFIG_PACKAGE_kmod-amneziawg=m" >> .config
          echo "CONFIG_PACKAGE_amneziawg-tools=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-amneziawg=y" >> .config
          
          # Принудительно включаем криптографию
          echo "CONFIG_PACKAGE_kmod-crypto-lib-chacha20=m" >> .config
          echo "CONFIG_PACKAGE_kmod-crypto-lib-chacha20poly1305=m" >> .config
          echo "CONFIG_PACKAGE_kmod-crypto-chacha20poly1305=m" >> .config

          # Обновляем конфиг
          make defconfig

          # 3. Компиляция
          echo "    > make tools/install"
          make tools/install -i -j `nproc`

          echo "    > make toolchain/install"
          make toolchain/install -i -j `nproc`

          echo "    > make target/linux/compile"
          make target/linux/compile -i -j `nproc` V=s

          # 4. Проверка vermagic
          if [ -f ./build_dir/target-*/linux-*/linux-*/.vermagic ]; then
            VERMAGIC=`cat ./build_dir/target-*/linux-*/linux-*/.vermagic`
            VERMAGIC_EXPECTED=${{ matrix.build_env.vermagic }}
            echo "Real Vermagic: $VERMAGIC"
            if [ "$VERMAGIC" != "$VERMAGIC_EXPECTED" ]; then
              echo "Vermagic mismatch: $VERMAGIC, expected $VERMAGIC_EXPECTED"
              # exit 1 # Раскомментируйте, когда будете уверены в хеше
            fi
          else
            echo "Error: .vermagic file not found after compile"
          fi
      - name: Build AmneziaWG
        run: |
          VERMAGIC=`cat ./build_dir/target-*/linux-*/linux-*/.vermagic`
          echo "Vermagic: $VERMAGIC"

          VERMAGIC_EXPECTED=${{ matrix.build_env.vermagic }}

          if [ "$VERMAGIC" != "$VERMAGIC_EXPECTED" ]; then
            echo Vermagic mismatch: $VERMAGIC, expected $VERMAGIC_EXPECTED
            exit 1
          fi

          # Ignore kmod build for some targets, replace with the awg-go
          make package/kmod-amneziawg/{clean,download,prepare} V=s || true
          make package/kmod-amneziawg/compile V=s || true

          make package/luci-app-amneziawg/{clean,download,prepare}
          make package/luci-app-amneziawg/compile V=s

          make V=s package/amneziawg-tools/{clean,download,prepare}
          make V=s package/amneziawg-tools/compile

      - name: Prepare artifacts
        run: |
          tag_name=${{  github.ref_name }}
          mkdir -p awgrelease
          postfix="${tag_name}_v${{ matrix.build_env.tag }}_${{ matrix.build_env.pkgarch}}_${{ matrix.build_env.target}}_${{ matrix.build_env.subtarget}}"
          cp bin/packages/${{ matrix.build_env.pkgarch }}/awgopenwrt/amneziawg-tools_*.ipk awgrelease/amneziawg-tools_${postfix}.ipk
          cp bin/packages/${{ matrix.build_env.pkgarch }}/awgopenwrt/luci-app-amneziawg_*.ipk awgrelease/luci-app-amneziawg_${postfix}.ipk
          cp bin/targets/${{ matrix.build_env.target}}/${{ matrix.build_env.subtarget}}/packages/kmod-amneziawg_*.ipk awgrelease/kmod-amneziawg_${postfix}.ipk

      - name: Release
        uses: softprops/action-gh-release@v1
        with:
          files: awgrelease/*.ipk

